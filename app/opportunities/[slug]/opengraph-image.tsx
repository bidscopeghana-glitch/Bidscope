import {ImageResponse} from "next/og";import {getPublicTender} from "@/lib/server/seo-opportunities";
export const alt="BidScope tender opportunity";export const size={width:1200,height:630};export const contentType="image/png";
export default async function Image({params}:{params:Promise<{slug:string}>}){const item=await getPublicTender((await params).slug);return new ImageResponse(<div style={{width:"100%",height:"100%",display:"flex",flexDirection:"column",justifyContent:"space-between",padding:"72px",background:"linear-gradient(135deg,#083f31,#165f48)",color:"white"}}><div style={{display:"flex",fontSize:28,fontWeight:800,color:"#e0bd69"}}>BidScope · Ghana procurement intelligence</div><div style={{display:"flex",flexDirection:"column"}}><div style={{display:"flex",fontSize:58,fontWeight:800,lineHeight:1.08,maxWidth:1000}}>{item?.title||"Tender opportunity"}</div><div style={{display:"flex",marginTop:28,fontSize:26,color:"#cde0d7"}}>{item?.category||"Procurement"} · {item?.region||item?.country||"Ghana"}</div></div><div style={{display:"flex",fontSize:22,color:"#a9c9ba"}}>Public preview · Protected source details remain available according to your BidScope access</div></div>,size)}
