import{createHash}from"node:crypto";import{z}from"zod";import{apiErrorResponse}from"@/lib/server/api-error";import{supabaseRest}from"@/lib/server/supabase-rest";
const touch=z.object({source:z.string().max(120).nullable().optional(),medium:z.string().max(120).nullable().optional(),campaign:z.string().max(200).nullable().optional()});
const schema=z.object({visitorId:z.string().uuid(),sessionId:z.string().uuid(),landingPath:z.string().startsWith("/").max(500),pagePath:z.string().startsWith("/").max(500),referrerHost:z.string().max(255).nullable().optional(),deviceType:z.enum(["mobile","tablet","desktop"]),first:touch,last:touch,eventName:z.enum(["sign_up","sign_in","tender_view","alert_created","subscription_started","subscription_paid","service_request","official_source_opened"]).optional(),valueMinor:z.number().int().nonnegative().optional(),currency:z.string().length(3).optional()});
export async function POST(request:Request){try{const input=schema.parse(await request.json());const visitorHash=createHash("sha256").update(`${input.visitorId}:${process.env.BIDSCOPE_INTERNAL_SECRET||"bidscope-seo"}`).digest("hex");const row={visitor_id_hash:visitorHash,session_id:input.sessionId,landing_path:input.landingPath,referrer_host:input.referrerHost||null,first_source:input.first.source||null,first_medium:input.first.medium||null,first_campaign:input.first.campaign||null,last_source:input.last.source||null,last_medium:input.last.medium||null,last_campaign:input.last.campaign||null,device_type:input.deviceType,consented:true,last_seen_at:new Date().toISOString(),metadata:{last_path:input.pagePath}};await supabaseRest(`seo_traffic_sessions?on_conflict=session_id`,{method:"POST",headers:{Prefer:"resolution=merge-duplicates"},body:JSON.stringify(row)});if(input.eventName)await supabaseRest("seo_conversion_events",{method:"POST",body:JSON.stringify({session_id:input.sessionId,event_name:input.eventName,page_path:input.pagePath,value_minor:input.valueMinor||null,currency:input.currency||"GHS"})});return new Response(null,{status:204})}catch(error){return apiErrorResponse(error)}}
